#!/usr/bin/env bash

# The next line updates PATH for the Google Cloud SDK.
if [ -f '~/workspace/google-cloud-sdk/path.bash.inc' ]; then source '~/workspace/google-cloud-sdk/path.bash.inc'; fi

# The next line enables shell command completion for gcloud.
if [ -f '~/workspace/google-cloud-sdk/completion.bash.inc' ]; then source '~/workspace/google-cloud-sdk/completion.bash.inc'; fi
