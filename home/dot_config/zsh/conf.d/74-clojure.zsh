# Clojure CLI — the `clojure`/`clj` binaries are installed by the package step and live
# on PATH; user-level deps.edn aliases come from ~/.clojure/deps.edn. Nothing to init here
# beyond ensuring rlwrap-backed `clj` is available, which the package provides.
:
