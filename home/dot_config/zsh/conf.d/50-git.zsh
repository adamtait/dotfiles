# Git shell helpers (the global excludesfile is set declaratively in ~/.gitconfig,
# not mutated at shell startup like the old git.sh did).

alias g='git'
alias gst='git status'
alias gb='git branch'
alias gco='git checkout'
alias gd='git diff'
alias glg='git lg'   # pretty graph alias defined in ~/.gitconfig
